import 'package:flutter/material.dart';

class CardExprole extends StatelessWidget {
  const CardExprole({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}